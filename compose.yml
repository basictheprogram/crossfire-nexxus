---
name: cross-fire_nexxus
services:
  mysql:
    container_name: nexxus-mysql
    image: mysql:lts
    restart: unless-stopped

    cap_add:
      - SYS_NICE

    command: >
      --general-log=0
      --slow-query-log=1
      --log-queries-not-using-indexes=1
      --long-query-time=10
      --max_allowed_packet=256M
      --net_read_timeout=600
      --net_write_timeout=600
      --wait_timeout=28800
      --interactive_timeout=28800

    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      TZ: ${TZ}
      MYSQL_INNODB_BUFFER_POOL_SIZE: 512M
      MYSQL_INNODB_LOG_FILE_SIZE: 128M

    volumes:
      - mysql_data:/var/lib/mysql
      - "./docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d:ro"
      - /dev/log:/dev/log

    ports:
      - "3306:3306"

    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    labels:
      - "traefik.enable=false"

    networks:
      - nexxus

  django:
    container_name: nexxus-django
    image: basictheprogram/crossfire-nexxus:latest
    restart: always

    depends_on:
      mysql:
        condition: service_healthy
      traefik:
        condition: service_healthy

    environment:
      # Django v5.2
      DEBUG: true
      SECRET_KEY: ${DJANGO_SECRET_KEY}
      ALLOWED_HOSTS: ${DJANGO_ALLOWED_HOSTS}
      # MySQL v8.4
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_HOST: ${MYSQL_HOST}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_HOST_PORT: ${MYSQL_HOST_PORT:-3306}
      # Email
      EMAIL_HOST: ${EMAIL_HOST}
      EMAIL_PORT: ${EMAIL_PORT}
      EMAIL_HOST_USER: ${EMAIL_HOST_USER}
      EMAIL_HOST_PASSWORD: ${EMAIL_HOST_PASSWORD}
      # Misc
      TZ: ${TZ}

    ports:
      - "8000:8000"

    volumes:
      - ./src/mock_CDN/static:/vol/static

    labels:
      - "traefik.enable=false"

<<<<<<< HEAD
      # Use HostRegexp to match optional "castle" subdomain (matches nexxus.cross-fire.org and nexxus.castle.cross-fire.org)
      - traefik.http.routers.nexxus-http.rule=HostRegexp(`nexxus{subdomain:(?:\.castle)?}.${DOMAIN}`) && (Path(`/meta_client.php`) || Path(`/meta_html.php`) || Path(`/meta_update.php`))
      - traefik.http.routers.nexxus-http.entrypoints=http
      - traefik.http.routers.nexxus-http.service=nexxus-service

      - traefik.http.routers.nexxus-https.rule=HostRegexp(`nexxus{subdomain:(?:\.castle)?}.${DOMAIN}`) && (Path(`/meta_client.php`) || Path(`/meta_html.php`) || Path(`/meta_update.php`))
      - traefik.http.routers.nexxus-https.entrypoints=https
      - traefik.http.routers.nexxus-https.tls=true
      - traefik.http.routers.nexxus-https.tls.certresolver=le
      - traefik.http.routers.nexxus-https.service=nexxus-service

      # Allow HTTPS /v3 paths
      - traefik.http.routers.nexxus-v3-https.rule=HostRegexp(`nexxus{subdomain:(?:\.castle)?}.${DOMAIN}`) && PathPrefix(`/v3`)
      - traefik.http.routers.nexxus-v3-https.entrypoints=https
      - traefik.http.routers.nexxus-v3-https.tls=true
      - traefik.http.routers.nexxus-v3-https.tls.certresolver=le
      - traefik.http.routers.nexxus-v3-https.service=nexxus-service

      # Static / all other HTTPS traffic
      - traefik.http.routers.nexxus-static.rule=HostRegexp(`nexxus{subdomain:(?:\.castle)?}.${DOMAIN}`)
      - traefik.http.routers.nexxus-static.entrypoints=https
      - traefik.http.routers.nexxus-static.tls=true
      - traefik.http.routers.nexxus-static.tls.certresolver=le
      - traefik.http.routers.nexxus-static.service=nexxus-service

      # Redirect HTTP → HTTPS
      - traefik.http.routers.nexxus-http-redir.rule=HostRegexp(`nexxus{subdomain:(?:\.castle)?}.${DOMAIN}`)
      - traefik.http.routers.nexxus-http-redir.entrypoints=http
      - traefik.http.routers.nexxus-http-redir.middlewares=https-redirect@docker

      # Define backend service port
      - traefik.http.services.nexxus-service.loadbalancer.server.port=8000
=======
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthcheck/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
>>>>>>> 9ce8414a2b2ecb852b14618575b73b3baa2486b6

    networks:
      - nexxus

networks:
  nexxus:
    name: nexxus
    driver: bridge

volumes:
  nexxus-mysql-data:
    driver: local
  traefik-public-certificates:
    driver: local

# export $(grep -v '^#' .env | xargs)
# https://traefik.dev.local/dashboard/
