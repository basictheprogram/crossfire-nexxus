{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Debug Django2",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/.venv"
            },
            "cwd": "${workspaceFolder}/src"
        }
    ]
}
